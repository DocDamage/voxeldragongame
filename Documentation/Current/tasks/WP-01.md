# WP-01 — one real terrain-provider proof

**Dependency:** WP-00 selected candidate plus actual approved material/resource content. **Scope:** Original G1; not the full region or all framework layers.

Read [the full G0/G1 assignment](../../DesignPack/docs/production/G0_G1_READINESS_AND_TERRAIN_PROOF.md), [editable-world rules](../../DesignPack/docs/systems/EDITABLE_WORLD_RULES.md), [save/control contract](../../DesignPack/docs/systems/SAVE_AND_CONTROL_CONTRACT.md) and applicable acceptance IDs. They contain the complete required cases; this packet is only a launch point.

Integrate the single selected provider through the smallest useful project seam. Replace/extend the source capability/request interface only where actual APIs demand it. Add authoritative edit, geometry/collision readiness and nav readiness callbacks; Queued is not completion. Do not invent provider callback names or grant rewards from the input event.

Prove smooth dig and add, a traversable cave, refilled blockage, nav on a newly exposed surface, stale route cancellation, finite depletion/yield with duplicate-callback protection, full-bag overflow, and coherent save/quit/reload. Include protected/occupied-fill envelopes, adjacent permitted excavation and bounded water-edge behavior. Use real materials/content; diagnostic instrumentation is not replacement art.

Exercise the declared repeated-edit and representative streaming sequence and record actual machine/settings/latency/payload observations. A small static floor, removed cube, material preview, boolean capability flag or native request-validation test cannot pass G1. A failed mandatory capability produces a reproducible blocker or a narrowly justified next candidate, not multiple permanent terrain stacks.

Stop after the scoped proof or reproduced blocker. Update only actually exercised acceptance statuses with real evidence. Keep production Region 01, broad Mutable creator, dragons, horror and vehicle implementation outside this task. The next independent approved gate is WP-02's real Mutable recipe proof.
